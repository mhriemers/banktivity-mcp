{
  "name": "banktivity-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces --if-present",
    "start": "npm run start -w banktivity-mcp"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
